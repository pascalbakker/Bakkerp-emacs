# -*- mode: snippet -*-
# name: GET One Endpoint
# uuid: 
# key: fgeto
# condition: t
# --
@app.get("/${1:resources}/{${2:id}}", response_model=${3:SchemaRead})
async def get_${4:resource}(
    ${2:id}: int, 
    db: AsyncSession = Depends(get_db)
):
    """Get a single ${4:resource} by ID."""
    db_obj = await db.get(models.${5:ModelName}, ${2:id})
    if not db_obj:
        raise HTTPException(status_code=404, detail="${5:ModelName} not found")
    return db_obj